# Handoff — 2026-09-24 client review fixes

Source: client review doc (Untitled-1/-3 attachments) covering CORS/reCAPTCHA, SEO, copy, and layout changes for `romanavitkova.cz`. Session used the grill-with-docs interview flow; all decisions below were confirmed with the user before implementation. Build verified (`npm run build` passes, no TS/lint errors).

## Done this session

**Backend / infra**
- `api/send-email.ts`: CORS now allowlists 3 origins (reflects request origin if allowed, else omits header): `https://romanavitkova.cz`, `https://www.romanavitkova.cz`, `https://rv-portfolio-sigma.vercel.app`. Previews intentionally NOT supported (confirmed with user).
- reCAPTCHA "Invalid domain for site key" — fixed externally by user in Google reCAPTCHA admin console (domain added). No code change was needed/possible for this.
- `TECHNICAL_DESCRIPTION.md` CORS line updated to match new allowlist behavior.

**SEO**
- `index.html`: canonical, og:url, og:image, twitter:image, and both `Person`/`LocalBusiness` JSON-LD `url`/`image` now point to `https://www.romanavitkova.cz/` (apex `romanavitkova.cz` 301-redirects to `www` on Vercel — confirmed via live fetch, so `www` is canonical).
- `public/robots.txt` and `public/sitemap.xml` domain updated to match.
- `index.html` `FAQPage` JSON-LD replaced — now exactly matches the 5 live FAQ items in `translations.ts` (previously had 6 different, stale questions).

**Copy / CTA consistency** (in `src/translations.ts`, `cs` block only — `en` block is dead code, never rendered, left untouched)
- Hero secondary CTA: text `'Jak spolupráce probíhá'` → `'Možnosti spolupráce'`; link target `#your-journey` → `#choose-your-path` ([Hero.tsx](../src/components/Hero.tsx)).
- All remaining "Mám zájem o spolupráci" CTA buttons renamed to "Chci úvodní konzultaci" (`chooseYourPath.cta`, `closingBanner.cta`; Hero's was already correct).
- `WhyStrength` reasons list corrected — 3 of 6 items didn't match the client's requested wording (found via exhaustive line-by-line diff, not initially caught): "Samostatnost v každém věku" → "Samostatnost v každé životní etapě"; "Investice do budoucího zdraví" and "Důvěra ve vlastní tělo" replaced with "Jistota ve vlastním těle" and "Odvaha zkoušet nové věci".

**Layout changes**
- `WhyStrength.tsx`: rewritten from card+subtext+quote layout to a 3×2 (2-col mobile) icon grid with `divide-x divide-y` hairlines, no boxes. Icons: `Zap, HeartPulse, UserCheck, ShieldCheck, Sparkles, Compass` (user-approved). Closing quote block removed entirely (data field also removed from translations).
- `Foundations.tsx` / `YourJourney.tsx`: closing statement paragraphs widened (`max-w-xl` → `max-w-3xl`) for the "two lines, wider, centered" effect requested.
- `YourJourney.tsx`: added missing closing CTA button ("Chci úvodní konzultaci", `INTAKE_FORM_URL`, opens in new tab like other CTAs). Added `cta` key to `yourJourney` translations.
- `ChooseYourPath.tsx`: removed "Nejoblíbenější" badge rendering (kept the orange border highlight via `path.popular`); `popularBadge` translation key now unused/dead (left in place, harmless). Added location line ("Form Factory Fitness Karlín, Praha" + `MapPin` icon) to the Osobní tréninky card only, via new `location?: string` field on that path object.
- `WhyIDoThis.tsx`: full rewrite —
  - Added `padel.png` photo on the left (was previously text-only, no image), faded into the dark section background on its right edge via new `.why-i-do-this-photo-fade` CSS class in `index.css` (mirrors the existing `.hero-photo-fade` pattern but fades right→dark instead of left→cream).
  - Bio copy replaced with the new "Jsem Romana, osobní trenérka a nutriční koučka..." draft (confirmed with user this replaces the old shorter bio, not additive).
  - Closing line ("Pro mě síla znamená svobodu – v těle i v životě.") now styled with a short orange top-line + larger serif (`font-display`) treatment instead of the old bold/italic paired lines.
  - New full-width "Vzdělání a odborný rozvoj" block added below the bio, two columns (heading+intro left / 6 certifications list right), separated by a top border. New `whyIDoThis.education` object in translations (`heading`, `intro`, `certifications[]`).
- `App.tsx`: section order changed to match client's requested order: `Hero → Manifesto → StuckInCircle → Foundations → WhyStrength → HowICanHelp → WhyIDoThis → YourJourney → ChooseYourPath → MovementShared → FAQ → Contact → ClosingBanner`. (Client's list omitted `Manifesto`; user confirmed it should stay in its current position right after Hero — this was a spec gap, not an instruction to remove it.)

## Explicitly confirmed as already-correct / no action needed
Cross-referencing the client's doc against the live site (browser-fetched, not just local code) found most items already resolved before this session: nav/footer language, `lang="cs"`, intro consultation duration (30 min, consistent), the one FAQ answer text change, Manifesto/StuckInCircle/Foundations copy, HowICanHelp's 6 topic texts, ChooseYourPath card copy/pricing table numbers/order. Do not re-touch these without new instructions.

## Not done — needs follow-up
- **Deployment verification**: confirm Vercel's production deployment for `romanavitkova.cz`/`www.romanavitkova.cz` is tracking `origin/main` after these changes are pushed (there was earlier confusion about a stale deployment — turned out to be resolved by the time of this session, but re-verify after pushing this batch).
- These changes are **committed to the working tree but not yet pushed/deployed** — verify with `git status` / `git log` before assuming they're live. Check reCAPTCHA still validates on both `romanavitkova.cz` and `www.romanavitkova.cz` once deployed.
- No `CONTEXT.md` or ADR updates were made — none of this session's changes met the ADR bar (all reversible, unsurprising, no real rejected alternative), and no new domain glossary terms were introduced.
